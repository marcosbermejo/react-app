export default interface Club {
  id: string
  name: string,
  image: string,
  phone?: string,
  email?: string,
  address?: string,
  postalCode?: string,
  city?: string,
  province?: string,
  president?: string
}